- [ ] sounds
- [ ] custom textures
- [ ] being able to rotate a block independent of the player's direction
- [ ] a pop animation when placing blocks
- [ ] take meshes from magicavoxel and split them into blocks, then export each face separately
      so "half of a machine" can be visible"
